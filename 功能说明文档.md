# RP · 星屿（rp-site）功能说明文档

> 基于项目源码与《项目书.md》（更新至 2026-08-30）整理。覆盖新站全部功能、接口配置方法与注意事项。
> 新站定位：**本地优先的 AI 角色扮演站——所有数据只存你的浏览器，服务器零用户数据。**

---

## 目录

1. [项目定位与技术架构](#一项目定位与技术架构)
2. [运行与部署](#二运行与部署)
3. [界面结构总览](#三界面结构总览)
4. [功能逐个介绍](#四功能逐个介绍)
   - 对话 / 角色卡管理 / 好感度 / 记忆系统 / UI 模板 / 卡片广场 / 广场后台 / 在线跑团 / AI 工作台 / 导入导出 / 预设 / 世界书 / 正则 / 用量统计 / 工具维护 / 主题
5. [接口配置详解（重点）](#五接口配置详解重点)
6. [自建服务端（广场服务 + 跑团中继）](#六自建服务端广场服务--跑团中继)
7. [数据存储与备份](#七数据存储与备份)
8. [注意事项汇总（必读）](#八注意事项汇总必读)

---

## 一、项目定位与技术架构

| 项 | 说明 |
|---|---|
| 形态 | Vue 3 + TypeScript + Vite 单页应用，`base: './'` 可部署到任意子路径 |
| 状态管理 | Pinia（4 个 store：chat / characters / personas / settings） |
| UI | Naive UI + 自定义设计系统，亮/暗双主题 |
| 本地存储 | Dexie.js（IndexedDB），库名 `RpSiteV2`，9 张表 |
| AI 通信 | 浏览器直连用户配置的 OpenAI 兼容 API（SSE 流式），Key 只存本机 |
| 服务端 | 可选自建：广场服务（Node 零依赖）+ 跑团中继（Node + ws），均为哑管道 |
| 安全渲染 | markdown-it + DOMPurify 消毒 + 沙箱 iframe |

**核心原则**：无账号、无遥测、服务器不保存任何用户数据。角色卡、聊天记录、API Key 全部在浏览器 IndexedDB 里，清浏览器数据 = 数据清空，**请定期用「导入 / 导出」页备份**。

与旧版（旧版）的关系：新站为从零原创实现（非旧版衍生），内置预设文本沿用旧版资源；两站备份文件可互导。

---

## 二、运行与部署

### 2.1 开发与构建

```bash
cd rp-site
npm install
npm run dev        # 开发模式，http://127.0.0.1:5273
npm run build      # 生产构建 → dist/
npm run preview    # 本地预览构建产物
npm run typecheck  # vue-tsc 类型检查
npm test           # Vitest 单元测试（157 项）
npm run server     # 跑团中继 ws://127.0.0.1:8787
npm run plaza      # 广场服务 http://0.0.0.0:8788
```

### 2.2 部署方式

- **双击打开**：`npm run build` 后直接双击 `dist/index.html` 即可使用（已配置相对路径）。
- **静态托管**：把 `dist/` 上传任意静态服务器；GitHub Pages 部署已内置工作流（推 main 自动发布，Settings → Pages → Source 选 GitHub Actions）。
- **注意**：纯静态托管**只包含前端**。在线跑团的中继和自建广场上传服务需要自己找台服务器跑（见第六节）。

### 2.3 CI

- `.github/workflows/ci.yml`：push/PR 自动执行类型检查 + 单元测试 + 生产构建。
- `.github/workflows/deploy.yml`：main 分支推送自动构建并发布 GitHub Pages。

---

## 三、界面结构总览

**左侧主导航**（9 个视图）：

| 导航项 | 视图 | 说明 |
|---|---|---|
| 聊天 | ChatView | 默认主界面（首屏唯一同步加载的视图，其余懒加载） |
| 角色卡管理 | CharactersView | 卡片库、导入导出、编辑 |
| 好感度 | AffinityView | 六维三轴关系档案 + 雷达图 |
| 记忆系统 | MemorySystemView | 双引擎记忆管理 |
| UI 模板 | UiTemplatesView | 全局模板管理与预览 |
| 卡片广场 | PlazaView | 浏览/导入/上传分享卡（零配置） |
| 在线跑团 | HallView | 多人跑团大厅与房间 |
| AI 工作台 | AiWorkshopView | AI 辅助生成四类资源 |
| 导入 / 导出 | DataView | 备份恢复、旧版迁移、JSONL 导入 |

**「更多」抽屉**（右侧滑出，左侧分类 + 右侧内容）：

| 分组 | 条目 |
|---|---|
| 对话 | 预设 / 世界书 / 正则（世界书与正则需先选择角色卡） |
| 数据 | 用量统计 / 工具维护 |
| 应用 | 语言模型 / 模型槽位 / 图片生成模型 / 视频生成模型 / 生成参数 / 聊天背景（点击直达设置面板对应区块） |
| 管理 | 管理员口令（解锁广场后台） |

侧边栏底部另有**主题切换**按钮（亮/暗一键切换）。

---

## 四、功能逐个介绍

### 4.1 对话（聊天）

- **SSE 流式回复**：逐字输出，思维链（CoT）自动折叠显示，正文与推理分离渲染。
- **消息树**：核心特色。重 roll 会生成**兄弟分支**而非覆盖，旧回复永不丢失；可在分支间自由切换；楼层号按当前链路编号。
- **消息操作**：编辑 / 删除 / **续写**（让 AI 接着写）/ **代入**（以角色身份发送消息，指令附带用户人设）。
- **三模型槽位**：主对话 / 备用 A / 备用 B，聊天页顶栏快捷切换器随时换模型。
- **生成参数**：温度、推理强度、上下文滑窗（条数）实时可调（见 5.2）。
- **图片附件**：消息可携带图片。
- **统计**：楼层数与字数统计。
- **搜索面板**：全文搜索，结果按链路楼层号定位并 `scrollIntoView` 滚动到对应楼层。
- **HTML 安全渲染**：AI 输出与旧版迁移的 HTML 消息经 DOMPurify 消毒后在沙箱 iframe 中渲染。
- **临时指令（slash command）**：以 `/` 开头的临时指令不进入常规对话流。
- **聊天背景**：以当前角色卡封面作为氛围背景，浓度（默认 30%）与模糊（默认 6px）可调，浓度 0 = 关闭。

### 4.2 角色卡管理

- **导入**：SillyTavern **v2/v3** PNG 卡（PNG tEXt chunk 读写）与 JSON 卡；7 个键位回退链查找 UI 模板（`extensions.legacy_ui_templates` → `extensions.ui_templates` → `data.uiTemplates` → … → 根层驼峰/下划线）；世界书（character_book）、正则（regex_scripts）随卡读入。
- **卡字段支持**：名称、描述、人设、场景、**备选开场白**（alternate_greetings，开聊时可选）、示例对话（mes_example）注入、`system_prompt` / `post_history_instructions` 覆盖、世界书、正则脚本、UI 模板、变量回写规则（`legacy_state_sync_rules`）。
- **导出**：PNG / JSON，与 SillyTavern 生态互通。
- **UI N 角标**：卡片列表标出该卡内嵌的 UI 模板数量。
- **入口联动**：角色卡管理页可一键跳转 AI 工作台生成新卡。

### 4.3 好感度系统（Behavior Engine）

按「会话 + 角色名」为每个 NPC 独立建档，卡片网格展示，含迷你雷达图。

- **六维三轴模型**（0–100，雷达图对轴两端 180° 对置）：
  - 关注轴：兴趣 ↔ 厌烦
  - 心动轴：吸引 ↔ 反感
  - 自在轴：信任 ↔ 尬尬
- **加权综合分** = 正面加权均值 − 负面加权均值 × 0.7。权重：信任 1.3、反感 1.3、厌烦 1.2、吸引 1.1、兴趣 1.0、尴尬 0.9（正负各 3.4 对称）。负面折减意味着一次尴尬不清空长期积累，但持续反感仍会压垮关系。
- **9 段关系阶段**（综合分降序查表）：挚爱 ≥75 → 亲密 ≥60 → 好友 ≥48 → 朋友 ≥34 → 熟识 ≥20 → 初识 ≥10 → 陌生 ≥0 → 警惕 ≥-30 → 敌对。新 NPC 从「陌生」起步。
- **4 级冲突联动**：决裂(Lv4) 强制「敌对」、冷战(Lv3) 至多「警惕」、争执(Lv2) 至多「初识」；冲突可按维度阈值自动推导，也可在弹窗手动覆盖（含「自动推导」档）。
- **AI 自主评判**：每轮分析最近楼层（默认 24 层），按三轴输出严格 JSON；与旧值 **60/40 平滑合并**防跳变；漏评维度沿用旧值。
- **维度独立原则**：六维互相独立，允许「爱恨交加」的矛盾组合，不强制此消彼长。
- **雷达图**：纯 SVG 蜉蛛网（内垂弦边六边形 + 渐变发光平滑数据面），上半圆为正面维度、下半圆为负面，图形向上鼓 = 关系向好。
- **编辑弹窗**：6 根滑条按三组对轴分组，拖动实时联动雷达图与阶段判定；支持删除档案。
- **状态行注入**：每 NPC 一行「阶段（综合分 + 行为提示）+ 六维数值 + 冲突等级」注入提示词，按阶段约束 NPC 言行分寸。

### 4.4 记忆系统

双引擎，解决长对话遗忘。记忆模式在高级设置中以**滑块**切换（总结模式 / 向量模式）。

- **每轮自动入库（对齐旧版 autoExtract 的"每轮提取"语义）**：每轮回复完成后自动触发——总结模式调记忆副模型把**本轮**提炼为记忆条目；向量模式把**本轮**按旧版规则分片 embedding 入库（分批 16 条）。二者受「记忆引擎 / 自动巡逻」总开关门控；20 楼巡逻仍保留，负责把"保留楼层"之外的老楼层沉淀为记忆。
- **总结模式（默认）**：AI 分块提炼记忆要点，全量注入（按字数预算分配）；支持**补录**——历史分块并发提炼。
- **向量模式（旧版同构）**：走 OpenAI 兼容 `/v1/embeddings` 生成文本嵌入，按余弦相似度检索与当前剧情最相关的 **Top-K** 条注入；历史记忆向量懒加载。保存规则对齐旧版——空行分段（长段 1800 按句切、小段合并至 400）、代码/HTML 行剔除（stripVectorMemoryCode 同构）、记忆内容 = `第 N 轮\n用户：…\n角色卡：…`（summary 截 900）、内容指纹去重；注入格式也对齐旧版：`<role_memory_vector_recall>` + `<memory_fragment turn="N" similarity="xx%">` XML 分片 + 三行说明文。
- **副模型**：总结提炼可用独立模型（可手输自定义模型名）。
- **自动巡逻提炼**：对话推进中自动提炼（默认开，每 20 楼巡逻一次，三档详略）。
- **保留楼层**：只保留最近 N 楼原文，更早楼层由记忆替代（默认 32 楼）。
- 默认参数见 5.4；向量模式对 API 的要求与回退行为见注意事项 #4。

### 4.5 UI 模板（角色卡自带交互式 HTML）

角色卡可内嵌交互式 HTML（手机 UI、状态面板、仪表盘、创建向导等），变量由 AI 回复实时驱动。

- **变量引擎**：`{{p_name}}` 占位符、`{{#each list}}…{{else}}…{{/each}}` 循环（最多 50 层嵌套）、`@index`/`@first`/`@last`/`@key`、`root.` 引用、`../` 父级、`#each … as alias` 别名，输出自动 HTML 转义。
- **沙箱渲染**：填充后的 HTML 在 iframe 沙箱中渲染，自动测量内容高度撑满（无滚动条）。
- **挂载**：启用的模板按 order 排序、top/bottom 分组，挂在**当前链路最后一条 AI 消息**上；开场白即第一条 AI 消息，第一楼就能看到；流式生成中隐藏。
- **AI 驱动更新**：每次请求把当前变量状态以 `<ui_template_state_context>` 注入 system；AI 在正文后输出 `<ui_template_updates>{"updates":[{"id":"模板id","variables":{"路径":"新值"}}]}</ui_template_updates>`；系统解析后按路径（如 `items.0.name`）更新会话级状态并重渲染。更新块从正文中剥离，不污染对话。
- **变量回写规则（state-sync，正则驱动）**：解析端不绑定单一格式。内置三种方言——旧版 原生 `<ui_template_updates>`、酒馆变量块 `<UpdateVariable>`（平铺 `{路径:值}` JSON，自动按路径路由到拥有该变量的模板）、酒馆宏 `{{setvar::路径::值}}`（默认关闭，防误伤正文）。卡可携带自定义规则（`extensions.legacy_state_sync_rules`）：正则匹配任意标签块 + 三种载荷方言 + 可选固定目标模板，在「UI 模板」页可视化管理（预设一键添加 / JSON 编辑），其他生态角色卡不改提示词也能接上回写闭环。对象值深合并（只传部分字段不丢兄弟键），数组与标量整体替换。
- **副模型兜底分析（对齐旧版二次分析管线）**：主模型回复未携带任何变量更新块时（卡自带格式太强时常见），后台自动用副模型按最近楼层补一次变量分析（流式通道，返回固定 `{"updates":[…]}` JSON），回写会话状态并刷新节点快照；会话级去重防连发叠调用。「UI 模板」页可开关并指定分析副模型（空 = 复用记忆副模型；两者都未配置则不兜底，**不默认占用主模型**）。对话页底部有变量更新状态条（副模型分析中 / 更新 N 项 / 无变化 / 未配置 / 失败原因）。
- **全链路流式**：所有模型调用（主回复 / 续写 / 代入 / 记忆提炼 / 好感度评判 / AI 工作台 / 模板兜底分析）统一走 SSE 流式通道——`chatOnce` 对外语义不变、内部流式累积，中转站不会再掐非流式长请求。
- **记忆副模型不回退主模型**：记忆自动巡逻、好感度自主评判、手动补录都在「总结模式副模型」未配置时直接跳过并提示，不再静默使用主模型。
- **分支一致性**：每条 AI 消息完成时把变量状态快照写入节点；重 roll / 切分支 / 删除节点时沿链回滚到对应快照。
- **交互桥接**：模板内 `data-slash` 按钮或 `window.triggerSlash(text)` 经 postMessage 桥接到聊天输入框自动发送（含同源校验）。
- **管理页**：全局模板列表，预览为变量填充后的真实效果。
- **整页 HTML 面板托管（默认开启）**：部分卡不让面板走模板变量，而是让主模型每轮自画 `<!DOCTYPE html>` 整页面板（单楼可达上百 KB，吃满输出 token 并撑爆上下文）。站点自动识别这类消息（无需配置）：识别到后①向主模型注入 `[UI Panel Policy]`——只写剧情正文、禁止再画整页面板；②历史里的面板楼层在发送时替换为纯文本摘要（保留金额/新闻等事实），不再整段进上下文；③面板本体交给副模型每轮重绘（保持结构/CSS/JS 不变、只更新数据），挂在最新回复下展示，模型选择链与变量补全一致（手动指定 > 记忆副模型 > 自动 flash），输出经校验，失败/截断自动沿用上一版；④当前面板的文字状态摘要注入 system，主模型写剧情时金额/日期不失忆。角色卡工坊可按卡关闭（`uiPanelAuxTakeover`，默认开）；重绘输出上限见设置 `panelAuxMaxTokens`（默认 16000）。未画面板的普通卡不受任何影响。
- **残余限制**：快照为整状态覆盖，不支持旧版 turn 级变量变更日志回放。

### 4.6 卡片广场（用户视图，零配置）

- 从远程索引（静态 JSON 清单）浏览社区分享卡，一键导入本地卡库。
- **上传卡片**：开放上传，任何人可传（无需口令），提交后进入**待审区**，提示「已提交审核：…（审核通过后上架）」，审核通过前不在公开列表。
- 默认索引与上传地址内置（指向站主服务器 `dfzjb.site`），访客**无任何配置界面**，开箱即用。
- 清单格式（自建广场时）：`[{ "name", "description", "tags": [...], "url": "cards/xxx.json" }]`，`url` 可为相对路径或绝对地址，指向 ST v2/v3 卡 JSON 或 PNG。

### 4.7 广场后台（站主专用独立页面）

入口：**「更多 → 管理 → 管理员口令」**——输入服务器 `PLAZA_TOKEN`，经 `GET /plaza/api/review` 校验（200=解锁，403=错误）后进入；侧边栏**没有**后台入口，也无 `?admin` 后门。已解锁的浏览器下次免输口令直达；可「清除本机口令」退出站主态（口令只存本机浏览器）。

后台功能：

- **配置**：广场索引地址 / 上传接口地址 / 管理口令。
- **审核**：待审列表（卡名/文件名/描述/提交时间/标签）+ **试卡**（先把待审卡导入本地试玩再决定）+ 拒绝 + 通过上架（上架后自动刷新公开清单）。
- **已上架管理**：公开清单逐条列出，带「下架」按钮。

### 4.8 在线跑团

多人房间 + AI 主持（KP）+ 表达式骰子。**中继端到端加密、零存储零日志，战役只存房主浏览器 IndexedDB。**

**大厅**：

- 房间卡片网格（标题/简介/封面/人数/上锁角标）；开放房间点击即入，上锁房间凭密码进入（密码不出本机，服务器只存其 SHA-256 散列做校验）。
- 工具栏：**模型**（跑团模型设置）/ 刷新 / **人设**（入场身份管理）/ **创建房间**。
- **「我的团」常驻侧栏面板**：列出你开过的战役（封面/房间码/最后游玩/剧情条数，存本机 IndexedDB），可恢复进房或删除；头部「保留 − N ＋」自选只保留最近 N 场（**0 = 全部保留**，默认），超出自动清理最旧。
- **创建房间双模式**（弹窗顶部滑块切换，选择记本机）：
  - **简洁**：房间名 + 简介 + 封面 + 上锁密码 + AI 辅助创作（一句话生成房间名/简介）。
  - **详细**：完整开团设定——规则系统（**自由团 / COC7th / DND5e / 自定义**，含检定约定与对应快捷骰）、时代背景、自定义基调标签（至多 4 个）、人数、KP 风格 3 档、世界观与舞台、模组梗概（KP 秘密）、**剧情模组（结构化，可选：从模组库挂载或导入 JSON）**、开场场景、**开场白**（建团后自动作为第一段旁白发到剧情流）、关键 NPC / 房规 / 内容红线（均逐条添加删减）、场景地图备注。AI 备团可从一句话构想生成全套设定。
- **中继模式**：创建弹窗「共享大厅 / 私人中继」滑块——共享 = 开在公共中继、公共大厅可见；私人 = 开在「我的中继」上、公共不可见、**只能凭邀请链接进入**。
- **邀请链接**：房间顶栏「邀请」按钮复制 `?join=房间码&relay=中继地址&l=1` 链接；打开链接自动直达跑团页弹进房窗（地址栏自动清理），成员无需预配中继。

**房间内**：

- **共享剧情流**：旁白 / 玩家发言 / 骰子 / 系统事件四类消息；KP 流式回复；`<roll>` 检定链（KP 正文请求检定 → 房主自动掷骰 → 结果回注）。
- **表达式骰子**：`XdY±Z` 任意表达式（如 `2d6+1d4-1`）；快捷骰按规则系统出（COC 1d100 系 / DND 1d20 系），双行显示表达式与自动推算结果范围，说明文案新人友好；明骰（所有人可见，系统随机不可改）。
- **分线系统**：全体主线（id `party`）内置；房主可开自定义线（个人线绑定成员 / 合作线只起名，上限 12 条），页签切换；每条线剧情互相隔离，KP 按线生成上下文；线可收线（置灰保留）/ 重开。**注意：房间单密钥，所有线全员可见（页签只是视图过滤，不是私密频道）。**
- **叙述者路由（自带 API）**：每条线可指派叙述者——旁白由该线叙述者的**本机 API** 生成，烧谁的 key 一目了然；缺省/全体线 = 房主。每次生成在本线发一条计费留痕（线名/叙述者/约 N tokens 估算）。叙述者掉线或没配 key 该线冻结（有明确提示）。
- **战局状态面板**（记忆/道具/当前区域）：KP 每轮输出 `<state>` JSON 自动维护（道具按名去重上限 30、记忆去重上限 20 超限丢最旧）；房主可手动登记/删除；底部常驻「当前区域」条，房主可原位编辑；随快照同步给成员只读。
- **剧情模组（结构化跑团模组，可选挂载）**：模组 = **大致剧情（章节大纲）+ 规划路线（分支）+ 结局表 + 命运转盘随机表**。挂载后 KP 提示词自动注入模组块（章节节拍为 KP 秘密，约定不向玩家剧透），KP 在正文末尾用 `<module>` JSON 上报进度（章节/天数/路线/旗标/结局，绝对值语义），房主解析合并后随战局状态广播——与 `<state>` 同一条管线，零额外调用。进度存在战局状态 `progress` 字段里，老存档缺省兼容。
  - **剧情进度面板**：当前章节 / 时间线天数 / 路线 / 关键旗标 / 结局图鉴（达成的点亮，终局出横幅，房主可「开新周目」重置进度）；房主可手动切章节、±天数、增删旗标，全员经 state 事件实时同步。
  - **命运转盘面板**：模组随机表按权重画扇区（`usage=create` 的出身表供开局抽取，`action` 为行动/遭遇表），任何人在本机转（动画只在本机），结果以 `wheel` 事件**明牌广播**进剧情流并持久化，KP 会读到并在剧情里结算加成。
  - **团设弹窗**展示模组公开面（名称/简介/规模），章节节拍与结局真相只进 KP 提示词。
  - 模组来源：AI 工作台「剧情模组」页签生成/导入/编辑（见 4.9），或直接导入他人导出的模组 JSON。
- **团设查看**：顶栏团设 chip（规则·人数·基调）点击开只读弹窗，房主/成员同源（经 E2EE 快照同步）。
- **可靠性**：流式看门狗（40 秒无续传判叙述者掉线并提示）；中途加入自动收到房主快照补发；修复过同一发言重复触发两次生成的 bug。
- **模型**：见 5.5 跑团模型设置。

**边界**：房主离开即房间关闭（快照续团属后续版本）；AI KP 与 AI 备团依赖房主端 API 配置（除非用叙述者路由）。

### 4.9 AI 工作台

独立全屏页面（四周留边），自然语言描述 → AI 生成 → 编辑 → 入库，五类资源：

- **角色卡**：一段话 → 名称/人设/性格/场景/开场白，可编辑后保存为新卡。
- **世界书**：描述需求 → 多条 Lorebook 条目（触发词、常驻标记），可编辑后追加到指定角色卡。
- **正则脚本**：描述转换需求 → 显示层/发送层正则（保留层配置），可编辑后追加。
- **UI 模板**：描述界面需求 → 自包含 HTML 模板（含 `{{变量}}` / `{{#each}}`），可编辑后追加。
- **剧情模组**：一句话构想 → 整套结构化跑团模组（章节大纲/路线分支/结局表/出身与行动转盘），清单式可视化编辑；**模组库**存本机 IndexedDB，支持导入/导出 JSON 分享；开团时在创建房间（详细模式）挂载。

工作台内可独立选择 AI 模型（默认用当前对话模型）。

### 4.10 导入 / 导出（数据页）

- **全库备份**：`{ format: 'rp-site-backup', version: 3, data: { characters, chats, personas, kv, settings, memories, affinity, usage, campaigns, modules } }`（旧备份缺 `modules` 时该表留空恢复）。
- **恢复**：⚠️ **清空全库后整体写入**（见注意事项 #14）。
- **旧版互导**：与旧版同构的 `legacy_backup_*.json` 一键导入恢复全部角色卡与聊天记录；新站导出的备份也能被旧版导入。
- **酒馆 JSONL**：SillyTavern 聊天记录 JSONL 导入。
- 迁移实现带归一化与回环测试（77 条消息 / 1 卡 / 主线 + 分支通过）。

### 4.11 预设（更多 → 预设）

- 内置 **15 条**预设组（破限 / 预注入 / 防抢话 / 防神化 / 防重复等旧版同款文本）。
- **内置优先**：内置预设默认**全部启用**（仅「第三人称」因与「第二人称」互斥默认停用），强制存在、不可删除，排序优先于自建条目；对内置条目的启停与内容修改会被保留，可一键「重置内置预设」恢复出厂。
- 角色卡携带的预设（`system_prompt` / `post_history_instructions`）、世界书与正则一律位于内置预设之后注入，冲突时以内置为准；内置预设所在的 system 块不经过发送层正则，卡带正则无法改写。
- 有序、可启停、支持**带角色**的提示词条目；可自建。

### 4.12 世界书编辑器（更多 → 世界书）

先选角色卡，再编辑该卡的条目：

- 触发关键词 + **过滤词**，AND / OR / **NOT** 逻辑组合
- 概率触发、扫描深度、**递归激活**（已激活内容作为下一轮扫描源）
- 常驻（constant）、插入位置/顺序/深度
- 可视化条目列表，启停管理

### 4.13 正则编辑器（更多 → 正则）

- **显示层正则**（渲染前处理 AI 输出）与**发送层正则**（发送前处理用户输入）
- 支持启停与排序；全局开关在设置中（`regexEnabled` 默认开）
- 导入 AI 工作台生成的正则会保留层配置（显示层/发送层勾选不丢失）

### 4.14 用量统计（更多 → 用量统计）

按日统计 token 用量（usage 表，随备份导出）。

### 4.15 工具维护（更多 → 工具维护）

正则工具、临时指令（slash）、token 估算、搜索面板入口；备份/恢复/清库操作也在此面板补全（覆盖全部 9 张表）。

### 4.16 主题

亮 / 暗双主题一键切换（侧边栏底部），默认浅色。

---

## 五、接口配置详解（重点）

所有模型配置入口：**「更多 → 应用」分组**（语言模型 / 模型槽位 / 图片生成模型 / 视频生成模型 / 生成参数 / 聊天背景）。改动自动防抖保存（350ms），无需保存按钮。

### 5.1 语言模型（对话主接口）

| 字段 | 默认值 | 说明 |
|---|---|---|
| Base URL | `https://dfzjb.site/v1` | OpenAI 兼容接口地址，**填到 `/v1` 为止**，不要带 `/chat/completions` |
| API Key | 空 | 密码框可点击显示；**仅存本机浏览器，不会上传** |
| 模型列表 | — | 填好 Key 后自动从 `/v1/models` 拉取（按 text/image/video/audio/embedding 分组显示）；也可点「获取/刷新模型列表」手动拉；所有选择器均为**可输入下拉**——列表里没有的模型名可直接手输 |

### 5.2 模型槽位与生成参数

**三槽位**：主对话 / 备用 A / 备用 B，各绑一个模型；**高亮槽位为当前对话使用的模型**，聊天页顶栏可快捷切换。

**生成参数**：

| 参数 | 范围 | 默认 | 说明 |
|---|---|---|---|
| 温度 | 0–2（步进 0.05） | 0.8 | 越低越稳定 |
| 最大 token | 64–128000 | 2048 | 单次回复上限 |
| 上下文条数 | 2–200 | 50 | 滑窗携带的近期消息条数 |
| 推理强度 | minimal / low / medium / high | medium | 思维链深度，minimal 最快 |

### 5.3 图片 / 视频生成模型（独立配置）

各一组 Base URL + API Key + 模型，**留空则复用对话 API 的地址与 Key**；配好后各自从 `/v1/models` 自动发现（也可手输）。当前用于后续的对话内配图、头像生成、文生视频/图生视频功能（**对话内多模态生成尚未接入，配置位已就绪**——见注意事项 #19）。

### 5.4 记忆系统参数（默认值）

| 参数 | 默认 | 说明 |
|---|---|---|
| 记忆引擎总开关 | 开 | |
| 模式 | 总结（summary） | 可切向量（vector） |
| 总结副模型 | 空 | 可手输 |
| embedding 模型 | `text-embedding-3-small` | 向量模式用，从模型列表选或手输 |
| 向量召回 Top-K | 8 | |
| 提炼并发 | 10 | 补录时分块并发数 |
| 保留楼层 | 32 | 更早楼层由记忆替代 |
| 记忆字数预算 | 1500 | 注入预算分配 |
| 自动巡逻 | 开 | 每 20 楼自动提炼 |
| 详略 | balanced | 三档 |

### 5.5 跑团模型设置与中继（大厅工具栏「模型」）

- **双模式**：「跟随主站（默认）」= 用「更多 → 语言模型」当前激活槽位；「跑团专用」= 为 KP 单独配 Base URL / API Key / 模型 + 温度（0.8）/ 最大 token（2048）/ 推理强度（medium）。三项齐全才启用专用配置，否则回退主站；底部实时显示「KP 将使用：…」。**只影响在线跑团，不影响主站对话。**
- **高级折叠区**：中继地址（「我的中继/私人中继」，留空 = 同源 `/ws`）与房主世界观备注。
- **共享中继**：默认 `wss://dfzjb.site/ws`（站主维护）——⚠️ 截至项目书 2026-08-30 记录**尚未部署成功**（nginx 301 循环），见注意事项 #5。
- AI 备团与 AI 辅助创作复用 KP 模型配置。

### 5.6 广场接口（站主在广场后台配置）

| 字段 | 默认值 |
|---|---|
| 广场索引地址 | `https://dfzjb.site/plaza/index.json` |
| 上传接口地址 | `https://dfzjb.site/plaza/api/cards` |
| 管理口令（plazaUploadToken） | 空，只存本机 |

自部署到其他域名的站主需在 `src/db.ts` 的 `DEFAULT_SETTINGS` 同步修改前两个默认值（重新构建）。

---

## 六、自建服务端（广场服务 + 跑团中继）

### 6.1 广场服务 `server/plaza.js`（零依赖 node:http）

```bash
npm run plaza                    # http://0.0.0.0:8788，托管 <仓库>/public/plaza
PLAZA_PORT=9000 PLAZA_DIR=/data/plaza PLAZA_TOKEN=口令 npm run plaza   # 全部可选
```

**环境变量**：

| 变量 | 默认 | 说明 |
|---|---|---|
| `PLAZA_PORT` | 8788 | 监听端口 |
| `PLAZA_DIR` | `<仓库>/public/plaza` | 数据目录（index.json + cards/ + pending/ + review.json） |
| `PLAZA_TOKEN` | 无 | **管理口令（审核/下架凭据），生产必配**——否则谁传的卡都上不了架 |
| `PLAZA_RATE_PER_HOUR` | 60 | 每 IP 上传限速 |
| `PLAZA_MAX_PENDING` | 200 | 待审队列上限 |

**HTTP 接口**：

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | `/plaza/index.json`、`/plaza/cards/*` | 无 | 公开清单与卡片（前端订阅的静态索引） |
| GET | `/plaza/api/ping` | 无 | 探活 |
| POST | `/plaza/api/cards` | **无需口令** | 开放上传。请求体 `{ filename, data(base64), name, description, tags }`；校验扩展名 / PNG 签名 / JSON 合法性 / ≤6MB；重名自动加序号；进待审区 `pending/`，**不公开、不进清单** |
| GET | `/plaza/api/review`、`/plaza/api/review/<id>` | `x-plaza-token` | 待审列表 / 取待审卡原文件 |
| POST | `/plaza/api/approve` / `/plaza/api/reject` | `x-plaza-token` | 上架（原子 rename 进 cards/ 并写 index.json）/ 拒绝删除 |
| POST | `/plaza/api/remove` | `x-plaza-token` | 下架已上架卡 |

内容安全三件套：开放上传但**先审后上架**、每 IP 限速、待审队列上限；`review.json` / `index.json` 损坏时拒绝写入（不覆盖原数据）。CORS 已放开（卡片是公开资源）。

**生产部署（systemd + nginx）**：`deploy/plaza.service`（改 `PLAZA_TOKEN` 与路径）+ `deploy/nginx-plaza.conf`（location 片段，**放宽 `client_max_body_size`** ≥6MB）。防火墙**不要**放行 8788，只走 nginx 443（`X-Forwarded-For` 是限速与审计依据）。**备份 = 打包 `PLAZA_DIR` 一个目录**。

### 6.2 跑团中继 `server/index.js`（Node + ws，仅一个依赖）

```bash
npm run server     # ws://127.0.0.1:8787
```

- **无状态哑管道**：房间创建/加入/列表、成员进出广播、定向与广播转发、密码校验（只存散列）、心跳剔僵、房主离开即关房；**零持久化、零日志**；房间消息端到端密文（服务器只见密文），房间元数据只存内存。
- 开发模式 `npm run dev` 已把 `/ws` 代理到 8787，开箱即用。
- 生产：nginx 反代 `/ws` 到 8787，**必须精确匹配 `location /ws`（不要目录式 `location /ws/`，会触发 301 循环）**，并带 `proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection "upgrade"; proxy_read_timeout 24h;`。

---

## 七、数据存储与备份

**IndexedDB（Dexie，库 `RpSiteV2`）9 张表**：

| 表 | 主键/索引 | 内容 |
|---|---|---|
| characters | uuid, name, createdAt | 角色卡 |
| chats | id, charUuid, updatedAt | 聊天会话（含消息树） |
| personas | uuid, name | 用户人设（跑团入场身份） |
| kv | key | 杂项键值 |
| settings | id | 全部设置（含 API Key，仅本机） |
| memories | id, sessionId, createdAt | 记忆条目 |
| affinity | id, sessionId | 好感度档案 |
| usage | date | 每日 token 用量 |
| campaigns | id, roomCode, updatedAt | 跑团战役（仅房主本地，Dexie v2 新增） |

- 写库前统一剥响应式 Proxy（`deepPlain`），避免 IndexedDB `DataCloneError` 静默拒绝。
- 备份/恢复/清库覆盖**全部 9 张表**。
- 设置读取做浅合并（`{ ...DEFAULT_SETTINGS, ...saved }`），升级版本旧存档天然兼容。

---

## 八、注意事项汇总（必读）

1. **数据只存浏览器**：无账号无云端。清理浏览器数据 / 换设备 / 重装系统 = 数据全丢。请定期在「导入 / 导出」页导出全库备份。
2. **API 地址格式**：OpenAI 兼容 base URL，填到 `/v1` 结尾；带 `/chat/completions` 会请求到错误路径。
3. **Key 安全**：Key 只存本机 IndexedDB，不会上传；但导出的备份文件里含 Key，**不要把备份发给陌生人**。
4. **向量记忆的前置条件**：需要 API 节点支持 `/v1/embeddings`；不支持时**自动回退总结模式**。首次切向量模式后历史记忆逐条懒加载嵌入，检索会稍慢属正常。
5. **共享中继未部署**：项目书记录 `wss://dfzjb.site/ws` 实测 nginx 301 循环（`/ws`↔`/ws/`），**共享大厅当前不可用**；修复方式见 6.2 的 nginx 精确匹配写法。在此之前在线跑团请自建中继或用同源 `/ws`。
6. **GitHub Pages 等纯静态托管不含中继**：在线跑团需要自行部署 `npm run server` 并反代 `/ws`（wss）。
7. **房主即服务器**：跑团战役只存房主浏览器；房主离开房间即关闭；中途加入靠房主快照补发。房主断线 = 团暂停。
8. **端到端加密边界**：房间码即密钥，中继只见密文；但**分线不是私密频道**（单密钥，所有线全员可见）；真正的暗骰/密聊未实现。
9. **叙述者掉线即线冻结**：分线指派叙述者后，该叙述者未配 API 或掉线，该线无法生成（有提示）；没有「房主 key 代跑」兜底开关。
10. **计费留痕是估算**：跑团计量按 ~2 字符/token 粗估，非服务商真实账单。
11. **跑团模型不影响主站**：「模型设置」的专用配置只对在线跑团生效；三项（URL/Key/模型）不齐全自动回退主站当前槽位。
12. **`PLAZA_TOKEN` 生产必配**：广场上传无需口令（先进待审区），但审核/上架/下架全靠它；不配置则没人能上架。它是管理口令，**不是**上传口令。
13. **广场安全部署**：8788 不对公网放行，只走 nginx 443；`client_max_body_size` 放宽到 ≥6MB；限速与审计依赖 `X-Forwarded-For`。
14. **恢复备份是全量覆盖**：`restoreAll` 会先**清空全部 9 张表**再写入；导入前确认备份是想要的版本，导入时不要同时操作其他页面。
15. **UI 模板快照为整状态覆盖**：分支回滚按快照恢复，无 turn 级变更日志回放；对现有使用无影响。
16. **楼层号按链路编号**：切分支后楼层号会按当前链路重新计数，搜索结果按链路定位。
17. **多模态尚未接入对话**：图片/视频模型配置位已就绪，但对话内配图、头像生成、文生视频仍在路线图 P1；配置了暂不会在聊天里生效。
18. **移动端在打磨中**（路线图 P2）；小屏可用但未专门优化。
19. **开发环境端口**：dev 5273 / 中继 8787 / 广场 8788；`npm run dev` 已内置 `/ws` 代理，本地联调开箱即用。
20. **协议**：新站代码为原创实现，最终开源协议待定；内置预设文本沿用旧版资源（旧版为 CC BY-NC 4.0，禁止商用）。

---

*文档基于代码库现状（2026-08-31 分析）与《项目书.md》（2026-08-30）编写。功能迭代后请以最新代码为准。*
